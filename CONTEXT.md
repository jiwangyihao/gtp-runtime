# 领域词汇

- **平台**：承载目标游戏、运行证据和社区翻译成果的产品整体。
- **目标游戏**：经显式允许、玩家希望阅读中文译文的游戏。
- **guest**：由容器基座承载、但未作为独立系统应用进程运行的应用实例。中文说明中保留 `guest`，避免与 Android 用户或访客账号混淆。
- **容器基座**：负责准备并运行 guest、维护其逻辑安装记录和协调系统交互的运行环境。
- **Kernel Identity**：内核对一次进程或 Binder 调用实际观察到的 Linux UID、PID 及进程代次。它不能由 guest 自报身份替代。
- **Guest Principal**：容器依据受信任安装记录和进程记录赋予 guest 的逻辑身份，包括包、证书、虚拟用户和进程关联；它不等同于 Kernel Identity。
- **声明身份**：APK Manifest、包名或调用参数声称的身份。声明身份本身不是调用者证明。
- **端点观察**：服务端在处理实际调用时取得的 Kernel Identity、包管理视图和调用结果。
- **installer attribution（安装来源归因）**：安装记录对安装方与发起方的归属描述；它是客户端决策输入，不等同于 Play ownership、许可证响应或 Integrity verdict。
- **Play ownership**：Google Play 账号与目标包之间由 Play 后端维护的取得/购买关系；安装来源字段不能证明该关系。
- **许可证响应**：许可端点针对一次真实请求返回的结果；它不能由安装来源字段或客户端本地分支推导。
- **Integrity verdict**：完整性服务对具体请求和运行环境返回的判定；它与安装来源、Play ownership 和许可证响应分别记录。
- **机制验证**：隔离一个接口、处理变量或调用链进行的可证伪实验；通过只证明该机制，不代表产品可交付。
- **历史旁路**：已证明某些独立机制但不再属于产品目标的实验路线；保留其证据，不继续扩展，也不用于替代主线验收。
- **兼容层**：在不改变上游安全或许可证语义的前提下，使目标游戏依赖的系统契约可被满足的模块。
- **Hook Runtime**：在受控 guest 进程中提供 Java/ART 与 native 观测、取文和呈现接入点的运行模块。
- **翻译载荷**：通过 Hook Runtime 取得文本片段并产生译文块的游戏相关逻辑；它不负责 GMS、账号或容器身份。
- **规则包**：可独立发布、审阅和升级的一组原文匹配与译文规则。
- **文本片段**：一次从目标游戏取得的、带屏幕位置或运行上下文的原文。
- **译文块**：与文本片段对应、供玩家阅读的目标语言文字。
- **架构闸门**：其证据结果决定某种系统拓扑能否继续采用的可证伪实验；闸门失败时必须停止对应路线。
- **证据**：由明确来源、版本、时间和观测方法支持的事实记录，不能由计划、推断或单个日志标签替代。
- **A 证据链**：许可证与 GMS 的独立验收链，包括真实身份、真实账号、真实服务绑定和真实后端结果。
- **B 证据链**：容器稳定性的独立验收链，包括进程、组件、渲染、输入、前后台和恢复行为。
- **C 证据链**：Unity/IL2CPP 翻译效果的独立验收链，包括实际取文、规则命中和译文呈现。
- **可交付**：A、B、C 中产品声明要求的全部证据均已在支持矩阵内通过，而非仅完成构建、注入或单次启动。
